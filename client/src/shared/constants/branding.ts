/** Default Mashtal profile / business cover when the user has not uploaded one. */
export const DEFAULT_BANNER_URL = '/images/mashtal-default-banner.png';

/** Shared public identity for every admin account (posts, chat, follow). */
export const MASHTAL_SUPPORT_NAME = 'Mashtal Support';
/** Served from the API server (`server/public/images`). */
export const MASHTAL_SUPPORT_AVATAR = '/images/mashtal-logo.png';
